import { Component, OnInit } from '@angular/core';

@Component({
  selector: 'bm-home', // NOTE selector not needed since referenced routing only
  templateUrl: `./home.component.html`
})
export class HomeComponent implements OnInit {

  constructor() { }

  ngOnInit() {
  }

}
